# Copia a .env.local y ajusta.

# 1 = carga perezosa (recomendado): sin projects/gazettes/JSON 2MB al arrancar
# 0 = legacy DataContext (Promise.all completo)
VITE_PERF_LAZY_DATA=1

# Polling de agenda en ms (solo activo en path lazy + pestaña visible + /agenda*)
# VITE_AGENDA_REFRESH_MS=60000
